package com.gerador.dietas;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertTrue;

class DietasApplicationTests {

    @Test
    void sanityCheck() {
        // Context load tests are covered by integration tests that require the database.
        // Unit tests (metabolism package) are added in Etapa 5.
        assertTrue(true);
    }
}
